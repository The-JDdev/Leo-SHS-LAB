package com.shslab.leo.executor

/**
 * CommandQueue — superseded by Level 5 Agentic Loop in ActionExecutor v3.
 * Retained as empty stub. Execution is now fully coroutine-based via executeAndReport().
 */
@Suppress("unused")
class CommandQueue {
    fun kill() {}
    fun reset() {}
    fun size(): Int = 0
}
